function isprime(userinput) {
    if (userinput == 1) {
       return false; 
    }
   else if (userinput == 2) 
   {
    return this;
        
    }
    var nsqrt = math.sqrt(userinput);
for (i = 2 ); i <nserinput>s 

}